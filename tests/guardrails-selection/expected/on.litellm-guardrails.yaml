# GENERATED by scripts/resolve-guardrails.sh from tests/guardrails-selection/fixtures/on.config.yaml (features.guardrails: true) — do not edit.
# Layered onto platform/litellm/values.yaml (ADR-0034): two always-on tenant-edge guardrails.
#   A) presidio-pii-mask     — Presidio masks PII in the prompt BEFORE it leaves LiteLLM (request continues).
#   B) prompt-injection-block — litellm_content_filter BLOCKs jailbreak/override prompts (HTTP 400).
# Presidio analyzer+anonymizer come from the `guardrails` catalog group (namespace guardrails).
proxy_config:
  guardrails:
    - guardrail_name: presidio-pii-mask
      litellm_params:
        guardrail: presidio
        mode: pre_call
        default_on: true
        presidio_analyzer_api_base: http://presidio-analyzer.guardrails.svc.cluster.local:3000/
        presidio_anonymizer_api_base: http://presidio-anonymizer.guardrails.svc.cluster.local:3000/
        pii_entities_config:
          EMAIL_ADDRESS: MASK
          PHONE_NUMBER: MASK
          CREDIT_CARD: MASK
          US_SSN: MASK
          IBAN_CODE: MASK
    - guardrail_name: prompt-injection-block
      litellm_params:
        guardrail: litellm_content_filter
        mode: pre_call
        default_on: true
        categories:
          - { category: prompt_injection_jailbreak, enabled: true, action: BLOCK, severity_threshold: medium }
          - { category: prompt_injection_system_prompt, enabled: true, action: BLOCK, severity_threshold: medium }
          - { category: prompt_injection_data_exfiltration, enabled: true, action: BLOCK, severity_threshold: medium }
