# GENERATED by scripts/resolve-profile.sh from tests/profile-selection/fixtures/cost.config.yaml (profile: cost) — do not edit.
# Last-applied Helm values overlay for the litellm release; overrides platform/litellm/values.yaml.
replicaCount: 1
redis:
  enabled: false
# Nest under proxy_config to deep-merge over the base proxy_config.general_settings; a top-level
# general_settings key is ignored by the chart, leaving prod silently fail-open.
proxy_config:
  general_settings:
    allow_requests_on_db_unavailable: true   # false = fail-closed budgets, prod only (ADR-0029 SR3)
