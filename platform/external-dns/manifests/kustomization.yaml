apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cloudflare token ExternalSecrets (external-dns + cert-manager namespaces). Each carries its own
# namespace — no transformer. Applied as a git-path source by the external-dns Argo app.
resources:
  - externalsecrets.yaml
