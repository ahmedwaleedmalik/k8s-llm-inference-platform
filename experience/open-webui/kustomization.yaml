apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: experience
# openwebui-secrets (OPENAI_API_KEY = minted LiteLLM vkey, WEBUI_SECRET_KEY = random) is created at
# deploy — kept out of git. Mint the vkey via LiteLLM /key/generate; generate WEBUI_SECRET_KEY randomly.
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - externalsecret.yaml
