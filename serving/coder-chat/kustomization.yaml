apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: serving
# Reuses the shared vllm-api-key Secret created by the serving-secrets app.
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
